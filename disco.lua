-- Created by Dibbydib (roblox/dibby_dib)
-- WARNING!! This one is untested. Excecute with caution.
while true do
game.Players.LocalPlayer.UpperTorso = BrickColor.new(math.random(), math.random(), math.random())
wait(0.1) 
game.Players.LocalPlayer.UpperTorso = BrickColor.new(math.random(), math.random(), math.random())
wait(0.1) 
end
